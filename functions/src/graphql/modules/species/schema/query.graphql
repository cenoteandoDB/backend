type Query {
  getSpecies(
    sort: SortField
    pagination: PaginationInput
    name: String
  ): SpeciesList!
  getSpeciesById(id: ID!): Species
  getSpeciesByGbifId(gbifId: ID!): Species
  getSpeciesByINaturalistId(inaturalistId: ID!): Species
}
