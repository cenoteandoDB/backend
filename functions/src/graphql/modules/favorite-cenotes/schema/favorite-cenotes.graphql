
scalar DateTime

type FavoriteCenotes {
  firestore_id: ID!
  userId: String!
  cenoteId: String!
  createdAt: DateTime
}

type FavoriteCenotesList {
  favoriteCenotes: [FavoriteCenotes!]!
  totalCount: Int!
}